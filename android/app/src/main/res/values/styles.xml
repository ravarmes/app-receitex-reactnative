<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>

        <!--
          Edge-to-edge é habilitado em tempo de execução via enableEdgeToEdge()
          na MainActivity. As cores/aparência das barras de status e navegação
          são controladas pelo componente <StatusBar> do React Native.
          Os atributos statusBarColor / navigationBarColor / windowTranslucentStatus
          foram removidos por serem descontinuados no Android 15 (SDK 35).
        -->
    </style>

</resources>
